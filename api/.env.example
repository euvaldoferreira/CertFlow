PORT=3000
API_KEY=troque-por-um-valor-aleatorio-longo

# Usada pela análise de IA (POST /api/analyze e auto-análise em POST /api/logs).
# Gere uma chave em https://aistudio.google.com/apikey — sem isso, os endpoints
# de log/download continuam funcionando normalmente, só a sugestão de
# seletores por IA fica indisponível.
GEMINI_API_KEY=...
